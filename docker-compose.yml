
services:
  zhengming:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - zhengming_data:/data
    restart: unless-stopped
    environment:
      - RUST_LOG=info
      - TANTIVY_PATH=/data/debate_index
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}

volumes:
  zhengming_data:
